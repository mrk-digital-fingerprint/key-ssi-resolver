require("../../../../../psknode/bundles/testsRuntime");
const assert = require("double-check").assert;
const KeySSIFactory = require("../../../lib/KeySSIs/KeySSIFactory");
const CryptoAlgorithmsRegistry = require("../../../lib/KeySSIs/CryptoAlgorithmsRegistry");

const RootSSI = require("../../../lib/KeySSIs/TreeSSIs/RootSSI");

// assert.callback("RootSSI successfull NodeSSI deterministic derivation", (callback) => {

// });

// assert.callback("RootSSI successfull multiple NodeSSIs deterministic derivation", (callback) => {

// });

// assert.callback("RootSSI successfull multiple NodeSSIs deterministic derivation starting at given index", (callback) => {

// });

// assert.callback("NodeSSI successfull NodeSSI deterministic derivation", (callback) => {

// });

// assert.callback("NodeSSI successfull multiple NodeSSIs deterministic derivation", (callback) => {

// });

// assert.callback("NodeSSI successfull multiple NodeSSIs deterministic derivation starting at given index", (callback) => {

// });

// assert.callback("Generate deterministically NodeSSI at position '0-1-3'", (callback) => {

// });
